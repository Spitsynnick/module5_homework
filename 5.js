const array1 = [1, 2, 3, true, "string"];

console.log("Количество элементов массива: " + array1.length); // длина массива

for (let i = 0; i < array1.length; i++) { // перебор массива и вывод в консоль каждого его элемента
  console.log(array1[i]);
};